# Design review

No frontmatter. The title comes from the H1, not from the dated filename.

- The sidebar keeps its own collapsed state
- The editor never owns navigation
- Conflicts surface as a banner, never as a silent overwrite
