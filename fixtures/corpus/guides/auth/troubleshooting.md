# Troubleshooting

No frontmatter, title from the H1.

- **401 on every request** - the clock on the client is skewed
- **403 after a rotation** - the old refresh token was replayed
- **Random sign-outs** - the session store is behind a cache
