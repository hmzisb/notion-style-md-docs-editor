/**
 * Bumped only on a breaking wire change. A backend reporting a higher major makes the
 * UI show the version-gap card instead of half-working (docs/03 section 9).
 */
export const CONTRACT_VERSION = 1;
